<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.feicui.edu.firstapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LiftcycleActivity" />
        <activity android:name=".ListViewArrayAdapterActivity"></activity>
        <activity android:name=".ListViewSimpleAdapterActivity">


        </activity>
        <activity android:name=".CustomAdapterActivity">
        </activity>
        <activity android:name=".GridActivity">
            <!-- android 第一个启动的界面 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>